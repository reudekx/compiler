1. 변수 선언, 함수 정의 등에서 타입을 어떻게 명시할지?

    C 스타일로?

2. 중괄호로 scope를 표현할지 아니면 do end 사용할지..

    일반 자판 배열으로는 do end가 오히려 타이핑하기 쉬우니까 do end?

    근데 나중에 매크로 등 메타 프로그래밍을 위한 문법 지원을 고려해보면 
    중괄호로 하는 게 편하지 않을까하는 막연한 생각이..

        -> 덤으로 세미콜론 등으로 표현식의 종료를 명시하는 게 파싱이 간편할 듯..

        그런데 어차피 단순 코드 치환이 아니라 파싱 트리에 접근한다든지 할 거면 어차피
        중괄호나 do end나 다를 바 없긴 하다.

3. 참조 타입을 도입해야할까? (즉 포인터와 구분되도록)

    Go는 참조 타입이 없다..
    하지만


    int a = 0;

    int add(int a, int b) {
        return a + b;
    }

    struct Point {
        int a;
        int b;
    }

2. 쉼표의 사용

    가령 열거형이나, 구조체 멤버 변수, 배열 등을 나타낼 때 쉼표를 사용할 것인지?

    [[[int; 5]; 4]; 3] arr = [1, 2, 3, 4, 5]

    int[3] arr2 = int[
        1
        2
        3
    ]

    Point p = {1, 2}